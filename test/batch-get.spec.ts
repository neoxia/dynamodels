/* eslint-env node, jest */
describe('The batch get method', () => {
  test.todo('should return all items when hash keys are corect [case < 100]');
  test.todo('should return all items when hash keys are corect [case > 100]');
  test.todo('should return all items when composite keys are corect [case < 100]');
  test.todo('should return all items when composite keys are corect [case > 100]');
  test.todo('should throw if a key or key pair is invalid');
});
