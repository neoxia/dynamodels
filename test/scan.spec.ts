describe('Class Scan<T>', () => {
  describe('Method filter', () => {
    test.todo('should add filtering options to scan paramters');
  });
  describe('Method paginate', () => {
    test.todo('should add pagination options to scan paramters');
  });
});
